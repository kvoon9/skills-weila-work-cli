# weila-work-cli

Use the weila-work-cli to manage Weila Work auth, org, address book, and groups through the management APIs.

This repository is auto-synced from `weila-work/.agents/skills/weila-work-cli`.

## Install

```bash
npx skills add kvoon9/skills-weila-work-cli
```
